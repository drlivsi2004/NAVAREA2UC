NAVAREA2UC
Recovery Era

MISSION

Do not propose fixes yet.

Your task is to VERIFY the Root Cause Analysis for:

1. XV-107/2026
2. XV-102/2026

The goal is to prove or disprove the current RCA using actual code paths, runtime traces and object counts.

====================================================
DEFECT XV-107/2026
====================================================

Observed:

Expected:
    1 Area

Actual:
    7 Labels

Current Hypothesis:

    handle_sublabels()
    captures the block before handle_area()

    area routing fails because
    "AREA TEMPORARILY DANGEROUS"
    does not match current area patterns.

Verification Steps:

1.
Identify ALL handlers that can process the block.

2.
Trace exact execution order.

3.
Show:

    handler called
    handler result
    accepted/rejected

4.
Determine:

    Does handle_area()

        execute?

    If yes:
        why does it fail?

    If no:
        why is it skipped?

5.
Provide actual values for:

    context_match
    area_match
    coord_count
    marker_count

6.
Show first location where Area candidate becomes Label candidates.

Required Output:

    Source
        ↓
    Extraction
        ↓
    Normalization
        ↓
    Routing
        ↓
    Construction
        ↓
    Export

Object count and geometry type at every step.

====================================================
DEFECT XV-102/2026
====================================================

Observed:

Expected:
    19 Labels

Actual:
    1 Label

Current Hypothesis:

    is_multi_point_navarea()
    returns False

    handle_multipoint()
    never executes

    handle_single_point()
    consumes first coordinate only.

Verification Steps:

1.
Trace all coordinates through pipeline.

2.
Report counts:

    Extraction
    Normalization
    Routing
    Construction
    Export

3.
Confirm:

    At what exact line
    19 coordinates become 1.

4.
Show values returned by:

    is_multi_point_navarea()

    handle_multipoint()

    handle_single_point()

5.
Verify that geometry loss occurs inside Routing
    and not later.

Required Output:

    Stage
    Coordinates Count
    Geometry Type

for every pipeline stage.

====================================================
GENERAL REQUIREMENTS
====================================================

Do not speculate.

Every statement must be backed by:

    code location
    condition
    variable value
    runtime evidence

For every conclusion provide:

    File
    Function
    Line Number

====================================================
DELIVERABLE
====================================================

For each defect provide:

1. Hypothesis Status

    CONFIRMED
    PARTIALLY CONFIRMED
    REJECTED

2. Root Cause

3. Exact Code Path

4. First Corrupting Step

5. Evidence

6. Proposed Minimal Fix

7. Regression Risks

IMPORTANT:

If current RCA is wrong,
identify the true root cause instead.

Do not stop at the first plausible explanation.

Keep tracing until the exact transforming statement
or decision point is located.
